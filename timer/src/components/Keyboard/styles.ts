import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    rowsContainer: {
        alignContent: "center",
        margin: 10
    },
    row: {
        flexDirection: "row"
    },
    buttonPlayContainer: {
        justifyContent: "center",
        alignItems: "center",
        height: 100   
    }
});